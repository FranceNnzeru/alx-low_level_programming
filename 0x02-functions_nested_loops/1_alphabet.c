#include "main.h"

/**
 * alphabet - prints all alphabet in lowercase followed by a new line
 *
 * Return - Always 0
 */

int void print_alphabet(void)
{
	int char = c;

	for (c = 'a'; c < 'z'; c++)
		_putchar(c);

	_putchar('\n');
}
